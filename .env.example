# ==================================================
# AI Breakup Recovery Agent - Environment Configuration
# ==================================================

# --------------------------------------------------
# API Keys (Required - Choose at least one)
# --------------------------------------------------

# Google Gemini API Key (Recommended for vision capabilities)
# Get your key from: https://makersuite.google.com/app/apikey
# Gemini keys typically start with "AIza"
GEMINI_API_KEY=AIza<your-gemini-api-key>

# OpenRouter API Key (Alternative LLM provider)
# Get your key from: https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-v1-<your-openrouter-api-key>

# --------------------------------------------------
# Memory Configuration (Optional)
# --------------------------------------------------

# Mem0 API Key for persistent memory across sessions
# Get your key from: https://app.mem0.ai/dashboard/api-keys
# MEM0_API_KEY=m0-<your-mem0-api-key>

# --------------------------------------------------
# Agent Configuration
# --------------------------------------------------

# Default model to use (if using OpenRouter)
MODEL_NAME=google/gemini-2.0-flash-exp

# Debug mode (true/false)
DEBUG_MODE=true
DEBUG_LEVEL=2

# --------------------------------------------------
# Bindu Directory Registration (Optional)
# --------------------------------------------------

# Agent metadata for Bindu Directory registration
AUTHOR_EMAIL=paras25619@gmail.com
AGENT_NAME=ai-breakup-recovery-agent
AGENT_VERSION=1.0.0

# --------------------------------------------------
# Auth0 Configuration (For UI Authentication)
# --------------------------------------------------

# These credentials are used by the UI to authenticate A2A API calls
# You can also configure these directly in the UI by clicking the "⚙️ Auth" button
# Only needed if you're using the Bindu web UI with authentication

AUTH0_DOMAIN=dev-<your-tenant>.us.auth0.com
AUTH0_CLIENT_ID=<your-client-id>
AUTH0_CLIENT_SECRET=<your-client-secret>

# Note: For the web UI, authentication is configured in the browser settings
# This .env file is just for reference when using the Python script:
# uv run get_auth0_token.py --domain $AUTH0_DOMAIN --client-id $AUTH0_CLIENT_ID --client-secret $AUTH0_CLIENT_SECRET --copy

# --------------------------------------------------
# Storage Configuration (For Bindu Platform)
# --------------------------------------------------

# Type: "postgres" for PostgreSQL or "memory" for in-memory storage
STORAGE_TYPE=memory
# DATABASE_URL=postgresql+asyncpg://<user>:<password>@<host>:<port>/<database>?ssl=require

# --------------------------------------------------
# Scheduler Configuration (For Bindu Platform)
# --------------------------------------------------

# Type: "redis" for distributed scheduling or "memory" for single-process
SCHEDULER_TYPE=memory
# REDIS_URL=rediss://default:<password>@<host>:<port>

# --------------------------------------------------
# Sentry Error Tracking (Optional)
# --------------------------------------------------

# Enable error tracking and performance monitoring
SENTRY_ENABLED=false
# SENTRY_DSN=https://<key>@<org-id>.ingest.sentry.io/<project-id>

# --------------------------------------------------
# OpenTelemetry Configuration (Optional)
# --------------------------------------------------

# Enable observability and tracing with Langfuse, Arize, or other OTEL platforms
TELEMETRY_ENABLED=false
OLTP_VERBOSE_LOGGING=false
OLTP_ENDPOINT="https://cloud.langfuse.com/api/public/otel/v1/traces"
OLTP_SERVICE_NAME=ai_breakup_recovery_agent
# OLTP_HEADERS={"Authorization":"Basic <base64-encoded-public-key:secret-key>"}

# --------------------------------------------------
# Example Configuration Sets
# --------------------------------------------------

# === Example: Gemini Only (Recommended) ===
# GEMINI_API_KEY=AIzaSyDummyExampleKey123456789
# MODEL_NAME=gemini-2.0-flash-exp
# DEBUG_MODE=true

# === Example: OpenRouter Only ===
# OPENROUTER_API_KEY=sk-or-v1-dummyexamplekey123456789
# MODEL_NAME=openai/gpt-4o
# DEBUG_MODE=true

# === Example: Full Production Setup ===
# GEMINI_API_KEY=AIzaSyDummyExampleKey123456789
# MEM0_API_KEY=m0-dummyexamplekey123456789
# STORAGE_TYPE=postgres
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/breakup_recovery
# SENTRY_ENABLED=true
# SENTRY_DSN=https://dummy@sentry.io/123
# DEBUG_MODE=false

# === Instructions ===
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace placeholder values with your actual API keys
# 3. At minimum, set GEMINI_API_KEY or OPENROUTER_API_KEY
# 4. Uncomment the configuration options you need
# 5. Never commit .env to version control (it's in .gitignore)
